interface:
  display_name: "Codex Memory Migrator"
  short_description: "Move Codex history and fix broken old paths"
  default_prompt: "Use $codex-memory-migrator to install the skill locally, export a Codex home snapshot, and rewrite old absolute paths for a new machine or user account."
